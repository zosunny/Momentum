// array의 목적은 하나의 변수안에 데이터의 리스트를 가지는 것!

const daysOfWeek = ["mon", "tue", "sed", "thu", "fri", "sat"];

console.log(daysOfWeek);

// Get Item from Array
console.log(daysOfWeek[5]);

// Add one more day to the array
daysOfWeek.push("sun")

console.log(daysOfWeek)